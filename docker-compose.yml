version: '3.8'

services:
  ushnotice:
    build: .
    container_name: ushnotice
    ports:
      - "8004:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
