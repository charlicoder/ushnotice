version: "3.9"

services:
  ushnotice:
    build: .
    container_name: ushnotice
    ports:
      - "8004:8000"
    env_file:
      - .env
    restart: unless-stopped

    networks:
      - ushspa-network

networks:
  ushspa-network:
    external: true